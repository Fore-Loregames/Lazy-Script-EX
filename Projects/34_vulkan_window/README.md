# 34 - Vulkan window

The first Vulkan backend example. The public code only selects Vulkan, opens an LSG window, begins a frame, and presents it. Instance creation, surface creation, device selection, swapchain images, command buffers, synchronization, and presentation stay inside LSG.
