# Experimental LazyUI node graph

This project is an experimental stress test for absolute layout, draggable cards, pins, connections, a palette, and a minimap.

It is included for compiler and UI regression testing, but it is not currently recommended as a production-quality node-editor reference. Use projects 28, 29, 30, and 32 as the primary LazyUI examples.
