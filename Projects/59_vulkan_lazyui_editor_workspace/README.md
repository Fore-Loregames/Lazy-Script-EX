# 59 - Vulkan LazyUI editor workspace

Vulkan parity copy of `30_lazyui_editor_workspace`. It uses the same LazyUI document, controls, text, images, clipping, canvas, and input code; only `LSG.use_vulkan()` selects the backend.
