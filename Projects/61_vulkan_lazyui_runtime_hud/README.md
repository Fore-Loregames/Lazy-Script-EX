# 61 - Vulkan LazyUI runtime HUD

Vulkan parity copy of `32_lazyui_runtime_hud`. It uses the same LazyUI document, controls, text, images, clipping, canvas, and input code; only `LSG.use_vulkan()` selects the backend.
