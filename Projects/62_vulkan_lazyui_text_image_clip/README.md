# Vulkan LazyUI text, image, clipping, and input

Focused Vulkan integration test for the actual retained LazyUI renderer. It exercises SDF text, generated RGBA texture upload, an oversized image clipped by a rounded panel, a button callback, and the normal document/input bridge in one Vulkan window.
