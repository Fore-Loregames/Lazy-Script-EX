# 35 - Vulkan LSSL triangle

Uses the same LSG window, LSSL shader object, mesh, bind, draw, and present flow as the OpenGL examples. LSSL compiles the shader to embedded SPIR-V and LSG creates the Vulkan pipeline internally.
