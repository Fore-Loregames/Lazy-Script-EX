# 60 - Vulkan LazyUI node graph

Vulkan parity copy of `31_lazyui_node_graph`. It uses the same LazyUI document, controls, text, images, clipping, canvas, and input code; only `LSG.use_vulkan()` selects the backend.
