# 57 - Vulkan LazyUI inline

Vulkan parity copy of `28_lazyui_inline`. It uses the same LazyUI document, controls, text, images, clipping, canvas, and input code; only `LSG.use_vulkan()` selects the backend.
