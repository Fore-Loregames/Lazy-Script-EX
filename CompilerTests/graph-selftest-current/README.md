# Experimental node-graph regression project

This project exists to exercise compiler and LazyUI behavior used by a large absolute-positioned graph: retained objects, dragging, pointer input, connections, clipping, scrolling, and cleanup.

It is a regression project rather than a beginner example or a production node editor.
