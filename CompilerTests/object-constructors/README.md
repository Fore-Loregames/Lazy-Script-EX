# Object constructor regression

Validates inferred constructor arguments, inherited object-table fields, an explicit parameterized `base.constructor(...)` call, nested object assignment, and `.new(...)` construction at O6.
